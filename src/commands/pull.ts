import { logger } from '../utils/logger';

export async function pullCommand(_resource: string, _name: string | undefined, _options: Record<string, unknown>): Promise<void> {
  logger.info('This command is not yet implemented.');
}
