import { logger } from '../utils/logger';

export async function syncCommand(_path: string | undefined, _options: Record<string, unknown>): Promise<void> {
  logger.info('This command is not yet implemented.');
}
