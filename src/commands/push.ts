import { logger } from '../utils/logger';

export async function pushCommand(_resource: string | undefined, _options: Record<string, unknown>): Promise<void> {
  logger.info('This command is not yet implemented.');
}
